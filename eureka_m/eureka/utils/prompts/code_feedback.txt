Please carefully analyze the policy feedback and provide a new, improved reward function that can better solve the task. Some helpful tips for analyzing the policy feedback:
    (1) If the success rates are always near zero, then you must rewrite the entire reward function
    (2) If the values for a certain reward component are near identical throughout, then this means RL is not able to optimize this component as it is written. You may consider
        (a) Changing its scale or the value of its sigma parameter (for bounded Gaussian rewards)
        (b) Re-writing the reward component 
        (c) Discarding the reward component
    (3) If some reward components' magnitude is significantly larger or shows numerical instability (e.g., values > 1000), then you must:
        (a) Use BOUNDED GAUSSIAN form: exp(-0.5 * (error/sigma)^2).clamp(0.0, 1.0) instead of unbounded exponentials
        (b) Re-scale its value to a proper range [0.0, 1.0] for each component
        (c) Clamp the final total reward to [0.0, 2.0] to match ground truth behavior
    (4) CRITICAL: If rewards are exploding (very large values), you MUST use bounded Gaussian rewards and clamp all components
Please analyze each existing reward component in the suggested manner above first, and then write the reward function code. 