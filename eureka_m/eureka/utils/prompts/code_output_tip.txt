The output of the reward function should consist of two items:
    (1) the total reward,
    (2) a dictionary of each individual reward component.
The code output should be formatted as a python code string: "```python ... ```".

MANDATORY STRUCTURE - DO NOT DEVIATE:
```python
@torch.jit.script
def compute_reward(
    obs: torch.Tensor
) -> Tuple[torch.Tensor, Dict[str, torch.Tensor]]:
    '''
    Reward function description here.
    '''
    # Your reward computation logic here
    
    rew_dict: Dict[str, torch.Tensor] = {
        "component1": component1_reward,
        "component2": component2_reward,
        "total_reward": total_reward
        # ... other components
    }
    
    return total_reward, rew_dict
```

CRITICAL PARAMETER REQUIREMENT:
    (0) Your function MUST take ONLY 'obs: torch.Tensor' as the single input parameter
        - DO NOT create separate parameters like piano_state, goal, etc.
        - Extract all components from 'obs' inside the function using slicing: component = obs[..., start:end]
        - Example: piano_state = obs[..., 0:88], goal = obs[..., 88:177], etc.

Some helpful tips for writing the reward function code:
    (1) You may find it helpful to normalize the reward to a fixed range by applying transformations like torch.exp to the overall reward or its components
    (2) If you choose to transform a reward component, then you must also introduce a temperature parameter inside the transformation function; this parameter must be a named variable in the reward function and it must not be an input variable. Each transformed reward component should have its own temperature variable
    (3) Extract observation components from 'obs' using slicing operations based on the observation structure provided
    (4) Most importantly, the reward function MUST have signature: def compute_reward(obs: torch.Tensor) -> ...
        The wrapper code can only provide 'obs' - it cannot provide decomposed parameters
    (5) ALWAYS include the @torch.jit.script decorator at the beginning
    (6) ALWAYS include proper type annotations for all parameters
    (7) ALWAYS return a tuple with (total_reward, reward_dict)
    (8) ALWAYS wrap your code in ```python ... ``` blocks
