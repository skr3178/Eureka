The output of the reward function should consist of two items:
    (1) the total reward,
    (2) a dictionary of each individual reward component.
The code output should be formatted as a python code string: "```python ... ```".

MANDATORY STRUCTURE - DO NOT DEVIATE:
```python
@torch.jit.script
def compute_reward(
    obs: torch.Tensor
) -> Tuple[torch.Tensor, Dict[str, torch.Tensor]]:
    '''
    Reward function description here.
    '''
    # Your reward computation logic here
    
    rew_dict: Dict[str, torch.Tensor] = {
        "component1": component1_reward,
        "component2": component2_reward,
        "total_reward": total_reward
        # ... other components
    }
    
    return total_reward, rew_dict
```

CRITICAL PARAMETER REQUIREMENT:
    (0) Your function MUST take ONLY 'obs: torch.Tensor' as the single input parameter
        - DO NOT create separate parameters like piano_state, goal, etc.
        - Extract all components from 'obs' inside the function using slicing: component = obs[..., start:end]
        - Example: piano_state = obs[..., 0:88], goal = obs[..., 88:177], etc.

Some helpful tips for writing the reward function code:
    (1) CRITICAL: Use BOUNDED GAUSSIAN rewards for error-based components to prevent reward explosion:
        - Instead of: reward = torch.exp(-temp * error)  # UNBOUNDED - DO NOT USE
        - Use: reward = torch.exp(-0.5 * torch.pow(error / sigma, 2)).clamp(0.0, 1.0)  # BOUNDED
        - Define sigma parameters (e.g., sigma = margin / 3.0) similar to ground truth tolerance functions
        - Example: key_sigma = torch.tensor(0.05 / 3.0, device=device) for key press accuracy
    (2) ALWAYS clamp individual reward components to [0.0, 1.0] before combining them
    (3) ALWAYS clamp the final total_reward to a bounded range (e.g., [0.0, 2.0]) to match ground truth behavior
    (4) RECOMMENDED: Include an ENERGY PENALTY component to encourage efficient movement:
        - Energy penalty scale: energy_penalty_coef = torch.tensor(5e-3, device=device)  # Typical value
        - If actuator power is in observation: energy_penalty = energy_penalty_coef * torch.sum(actuator_power, dim=-1)
        - If using actions: energy_penalty = energy_penalty_coef * torch.sum(actions ** 2, dim=-1)
        - Subtract from total: total_reward = positive_rewards - energy_penalty
        - Keep energy penalty small (5e-3 to 1e-2) so it doesn't dominate the reward signal
    (5) Extract observation components from 'obs' using slicing operations based on the observation structure provided
    (6) Most importantly, the reward function MUST have signature: def compute_reward(obs: torch.Tensor) -> ...
        The wrapper code can only provide 'obs' - it cannot provide decomposed parameters
    (7) ALWAYS include the @torch.jit.script decorator at the beginning
    (8) ALWAYS include proper type annotations for all parameters
    (9) ALWAYS return a tuple with (total_reward, reward_dict)
    (10) ALWAYS wrap your code in ```python ... ``` blocks
